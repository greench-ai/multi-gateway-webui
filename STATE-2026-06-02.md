# State — 2026-06-02 01:42 — Option 2 active

## Running

- **WebUI:** `http://localhost:18790/` (PID 851862, vite preview, 0.0.0.0:18790)
- **Bundle:** `index-CpqIj7o4.js` (79.31 kB), includes:
  - Pairing-required state detection
  - Approve-command UI in gateway cards
  - Reconnect (↻) button per card
  - Auto-seeded list of 9 active agents on first load
  - 3 sativabox agents intentionally omitted

## Active agents (9)

| Agent | Machine | Framework | Port | Status |
|-------|---------|-----------|------|--------|
| Kojiro | greench WSL2 | OpenClaw | 18789 | pairing-required |
| Fuma | greench WSL2 | NexisClaw | 19500 | pairing-required |
| Sasuke | greench-1 | GreenchClaw | 18420 | pairing-required |
| Akuma | acer | OpenClaw | 18789 | pairing-required |
| Gohan | acer | GreenchClaw | 18420 | pairing-required |
| Naruto | aspire | OpenClaw | 18789 | pairing-required |
| Goten | aspire | GreenchClaw | 18420 | pairing-required |
| Akira | storage | GreenchClaw | 18420 | pairing-required |
| Goku | storage | NexisClaw | 19500 | pairing-required |

## SSH tunnels (from prior turn)

4 systemd user services running, all `Restart=always`:
- 18021 → acer (Tailscale 100.124.148.81)
- 18022 → greench-1 (100.82.67.48)
- 18023 → storage (100.114.208.83)
- 18024 → sativabox (100.110.70.67, no GreenchClaw installed — placeholder)

## Disabled cron

`055780c8-2ad8-4bcb-926a-7d4d36844f4a` (HubClaw Gohan monitor) — disabled.

## User flow

1. Open `http://localhost:18790/` in a browser
2. See 9 orange "pairing-required" cards
3. For each, click 📋 to copy the approve command
4. Run it in a terminal on aspire
5. Click ↻ on the card to retry
6. Card turns green when paired

## Next: rooms

After all 9 are green, the user can ask me to add:
- Per-agent chat panels (already exist; need to wire selection)
- Rooms — group multiple agents in one chat, fan-out a single message to all
- Cross-agent coordination (Brainclaw hub integration)

## 2026-06-02 23:55 UTC — F1 Bundle Token Strip Fix (LIVE)

**Author:** Gohan 🔓
**Plan:** `/home/greench/shared/knowledge/research/hubclaw-bundle-fix-plan-2026-06-02.md` (Option A1)
**Audit:** `/home/greench/shared/knowledge/research/hubclaw-greench-ai-net-deep-analysis-2026-06-02.md` (F1)

### What shipped
- `vite-plugins/strip-seed-tokens.ts` — Vite plugin, `transform` hook rewrites
  `seed-gateways.ts` at build time, replacing `token:"..."` with `token:""`.
  Source file unchanged on disk. Bundle no longer ships plaintext tokens.
- `vite.config.ts` — wired the plugin into `plugins: [stripSeedTokens()]`.
- `src/components/multi-gateway-app.ts` — new `pendingTokenGateways` queue +
  `inTokenPromptFlow` flag. On boot, when a known seed gateway has no token,
  the edit modal auto-opens for it. After save, advances to the next. UX:
  user pastes 9 tokens once per session via the existing gateway-form.

### Verification (live at https://hubclaw.greench-ai.net/)
- Main bundle: `assets/index-DzfkRZgi.js` (117.71 kB)
- Seed chunk:  `assets/seed-gateways-C1qNUvD7.js` (1.11 kB; was 1.55 kB with tokens)
- Token audit: 0 plaintext tokens in either bundle. 9 empty `token:""` fields
  in seed chunk (matching the 9 known agents).
- Old hash (`iE_Jwdk2.js`) now returns SPA fallback HTML (1587 bytes) — tokens
  no longer fetchable from that URL.

### Git
- Branch: `fix/strip-seed-tokens` → merged into `main` (merge commit `fa2878a`)
- Commit: `5fa92e9 Strip seed gateway tokens from public bundle (F1 fix)`

### What user will see
- Next visit to https://hubclaw.greench-ai.net/ will trigger the edit modal
  for the first of 9 known seed gateways (each card with empty token).
- User pastes a token, clicks Save, modal advances to next gateway.
- After all 9 are entered, modal closes, all cards connect.
- Tokens are in-memory only (per existing security model) — re-paste on
  page reload, OR close & re-open the WebUI (whichever is sooner).

### Backlog (still open from the audit)
- F6: HSTS + X-Frame-Options via Cloudflare Transform Rules
- F8: Archive old `hubclaw/server.js` (port 3099, not running) to `archive/`
- F9: Strip sensitive fields from `lastMessage` debug stash
- F10: Gate `console.log("[ws msg]", ...)` behind `import.meta.env.DEV`
- A2 future: Encrypted IDB token storage with passphrase (multi-day work)
